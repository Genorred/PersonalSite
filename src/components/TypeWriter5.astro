<div class="split-appear">
  <h1 aria-hidden="true">
    <span class="split-left">
      <span class="inner inner-right">АХАХАХАХАХАХАХА</span>
    </span>
    <span class="separater"></span>
    <span class="split-right">
      <span class="inner inner-left">АХАХАХАХАХАХАХА</span>
    </span>
    <!-- accessible copy for screen readers -->
    <span class="visually-hidden">АХАХАХАХАХАХАХ</span>
  </h1>
</div>

<style>
  /* layout */
  /* layout */
  .split-appear {
    width: 100vw;
    position: absolute;
    top: 15%;
    left: 50%;
    transform: translateX(-50%);
    z-index: 1;
    overflow: hidden; /* ← prevents early overflow during reveal */
  }
  .separater {
    min-width: 56rem;
    padding: 2rem;
    display: inline-block;
  }
  .split-appear h1 {
    display: flex;
    justify-content: space-between;
    position: relative;
    margin: 0;
    font-family: monospace;
    font-size: 6rem;
    color: white;
    white-space: nowrap;
    line-height: 1;
  }
  /* common */
  .split-left,
  .split-right {
    display: inline-block;
    overflow: hidden;
    white-space: nowrap;
    pointer-events: none;
    will-change: width, transform; /* better animation perf */
  }
  .split-left {
    margin-right: auto;
    text-align: left;
  }
  .split-right {
  }
  .inner {
    display: inline-block;
    white-space: nowrap;
  }
  /* ───────────────────────────────────────
   Phase 1: Reveal (0–3 seconds)
   Phase 2: Infinite slide apart (starts at 3s)
──────────────────────────────────────── */
  .split-left {
    width: 0;
    animation: reveal-left 0.5s 1.5s steps(30, end) forwards;
  }
  .split-right {
    width: 0;
    animation: reveal-right 0.5s 1.5s linear forwards;
  }
  .inner-left {
    animation: scroll-left 0.1s 1.7s linear infinite;
  }
  .inner-right {
    text-align: right;
    animation: scroll-right 0.1s 1.7s steps(30, end) infinite;
  }
  @keyframes reveal-left {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }
  @keyframes reveal-right {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }
  /* After reveal → seamless infinite scrolling for slide apart */
  @keyframes scroll-left {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-7.2rem);
    }
  }
  @keyframes scroll-right {
    from {
      transform: translateX(-7.2rem);
    }
    to {
      transform: translateX(0%);
    }
  }
  /* Screen reader only */
  .visually-hidden {
    position: absolute !important;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0 0 0 0);
    white-space: nowrap;
    border: 0;
  }
</style>
