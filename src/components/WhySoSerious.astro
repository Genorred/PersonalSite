<div class="whySoSerouius">
  <div class="why">Why</div>
  <div class="so">So</div>
  <div class="serious">Serious</div>
</div>
<style>
  .whySoSerouius > * {
    opacity: 0;
    position: absolute;
    z-index: 10;
    top: 50vh;
    left: 50vw;
    transform: translate(-50%, -50%);
    width: calc(100vw - 3rem);
    padding: 2rem;
    font-size: 6rem;
    border: 4px solid;
    border-radius: 1rem;
    text-align: center;
  }
  .why {
    color: orange;
    animation: whyAppear 0.5s 2s;
    mix-blend-mode: difference;
  }
  .so {
    color: orange;
    animation: whyAppear 0.5s 2.5s;
    mix-blend-mode: difference;
  }
  .serious {
    color: orange;
    animation: whyAppear 0.5s 3s;
    mix-blend-mode: difference;
  }
  @keyframes whyAppear {
    from {
      /* background-color: var(--header-style-primaty); */
      background-color: oklch(0.145 0 0);
      background-image: radial-gradient(
        ellipse 80% 80% at 50% -20%,
        rgba(120, 119, 198, 0.3),
        rgba(255, 255, 255, 0)
      );
      opacity: 1;
    }
    to {
      opacity: 1;
      background-color: oklch(0.145 0 0);
      background-image: radial-gradient(
        ellipse 80% 80% at 50% -20%,
        rgba(120, 119, 198, 0.3),
        rgba(255, 255, 255, 0)
      );
    }
  }
</style>
