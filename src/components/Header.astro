<header class="header">
  <h1>Genorred \\ Internet Hero \\</h1>
  <div class="respectButtonWrap">
    <!-- <button class="respectButton"> -->
    <!--   <div class="respectButtonDecorations"> -->
    <!--     <div class="respectButtonDecorationsAnimator"> -->
    <!--       <div class="respectButtonDecoration"></div> -->
    <!--       <div class="respectButtonDecoration"></div> -->
    <!--     </div> -->
    <!--   </div> -->
    <!--   <a href="/why-respect"> Why respect ME </a> -->
    <!-- </button> -->
    <a href="/why-respect" class="respectButton">
      <div class="respectButtonDecorations">
        <div class="respectButtonDecorationsAnimator">
          <div class="respectButtonDecoration"></div>
          <div class="respectButtonDecoration"></div>
        </div>
      </div>

      <span>Why respect ME</span>
    </a>
  </div>
</header>

<style>
  .respectButtonWrap {
    display: flex;
    justify-content: center;
  }
  .header {
    background-color: var(--header-style-primaty);
    padding-bottom: 0.5rem;
  }
  .header > h1 {
    animation: subtleHorizontalShiftMain 6s ease-in-out infinite;
  }
  @keyframes slideRight {
    0%,
    80% {
      left: -110%; /* старт за пределами кнопки слева */
    }
    100% {
      left: 100%; /* уходит за пределы справа */
    }
  }
  .respectButtonDecorations {
    position: absolute;
    inset: -5px;
    clip-path: polygon(0% 0%, 100% 0%, 100% 2%, 2% 2%, 2% 98%, 0% 100%);
    pointer-events: none;
    overflow: clip;
  }
  .respectButtonDecoration {
    transform: rotate(15deg);
    background-color: var(--purple);
    height: 100%;
    width: 3rem;
  }
  .respectButtonDecorationsAnimator {
    display: flex;
    justify-content: space-between;
    height: 200%;
    width: 100%;
    position: absolute;
    left: 0%;
    top: -50%;
    animation: slideRight 10s ease-in -5s infinite;
  }

  @keyframes subtleHorizontalShiftMain {
    0% {
      transform: translateX(0);
    }
    25% {
      transform: translateX(-0.3px);
    }
    50% {
      transform: translateX(0.3px);
    }
    75% {
      transform: translateX(-0.2px);
    }
    100% {
      transform: translateX(0);
    }
  }
  /* .respectButton > span { */
  /*   position: relative; */
  /*   z-index: 2; */
  /*   mix-blend-mode: multiply; */
  /* } */
  .respectButton {
    display: inline-flex;
    margin-block: auto;
    align-items: center;
    justify-content: center;

    padding: 1rem 2rem;

    background: transparent;
    border: 2px solid var(--purple);
    /* color: var(--purple); */

    text-decoration: none;
    font-weight: 600;
    letter-spacing: 0.05em;

    position: relative;
    transition:
      background-color 0.25s ease,
      color 0.25s ease,
      box-shadow 0.25s ease,
      transform 0.1s ease;
  }
  .respectButton:hover {
    background: var(--purple);
    /* color: white; */
  }
  .respectButton:active {
    transform: translateY(2px);
  }
  .respectButton:focus-visible {
    outline: 3px solid rgba(128, 0, 255, 0.6);
    outline-offset: 4px;
  }
</style>
